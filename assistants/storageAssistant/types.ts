export enum StoreFailure {
    unknown,
    keyAlreadyExists,
}

export enum RetrieveFailure {
    unknown,
    keyDoesNotExists,
}

export enum UpdateFailure {
    unknown,
    keyDoesNotExists,
}

export enum DeleteFailure {
    unknown,
    keyDoesNotExists,
}
